class Solution(object):
    def smallerNumbersThanCurrent(self, nums):
        arr = []
        
        n = len(nums)
        for i in range(n):
            count = n
            for j in range(n):
                if nums[i] <= nums[j]:
                    count -= 1
            arr.append(count)
        return arr
