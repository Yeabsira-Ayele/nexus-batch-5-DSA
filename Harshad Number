class Solution(object):
    def sumOfTheDigitsOfHarshadNumber(self, x):
        if x < 2:
            return x

        y = str(x)
        z = 0
        for i in range(len(y)):
            z += int(y[i])
            print(z)
        if x % z == 0 :
            return z
        return -1
