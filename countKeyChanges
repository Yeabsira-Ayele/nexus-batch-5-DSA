class Solution(object):
    def countKeyChanges(self, s):
        count = 0 
        for x in range(len(s) - 1):
            if s[x].lower() != s[x + 1].lower():
                count +=1
        return count
