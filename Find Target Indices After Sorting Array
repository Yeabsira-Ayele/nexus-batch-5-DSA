class Solution(object):
    def targetIndices(self, nums, target):
        nums.sort()
        lst = []
        n = len(nums)
        for i in range(n):
            if  target == nums[i]:
                lst.append(i)
        return lst
