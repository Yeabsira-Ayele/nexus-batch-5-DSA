class Solution(object):
    def findWordsContaining(self, words, x):
        count = []
        for i  in range(len(words)):
            if x in words[i]:
                count.append(i)
        return count

        
